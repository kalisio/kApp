version: '3.3'

services:
  testcafe:
    image: kalisio/testcafe:1.3.3
    depends_on:
      - mongodb
      - app
    environment:
      - NODE_ENV=production
      - DB_URL=mongodb://mongodb:27017/${APP}
      - APP_URL=app:${PORT}
    volumes:
      - ../test:/tests
     - ../screenshots:/screenshots
    #command: chromium:headless /tests/*.test.js #firefox /tests/*.test.js -S -s screenshots
    command: firefox:headless /tests/*.test.js -S -s /screenshots
    networks:
      - app-network
      
networks:
  app-network:
    external:
      name: ${DOCKER_NETWORK}